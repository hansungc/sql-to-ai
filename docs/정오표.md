# 정오표

『SQL부터 AI까지 데이터 분석 With 클로드 코드』(조한성 지음, 정보문화사)의 지면 오류를
확인되는 대로 여기에 모읍니다. 다음 쇄에 반영됩니다.

## 1쇄

| 쪽 | 위치 | 인쇄된 내용 | 바른 내용 |
|---:|---|---|---|
| 436 | 환경 셋업 · 행 수 확인 SQL | `` SELECT `artists` AS table_name `` (백틱) | `SELECT 'artists' AS table_name` (작은따옴표) |
| 436 | 같은 SQL 3행 끝 | `... FROM tracks\` (역슬래시) | `... FROM tracks` |

**설명.** 여덟 개 테이블 이름이 문자열이 아니라 백틱으로 조판됐습니다. MySQL은 백틱을
칼럼 이름으로 읽기 때문에 그대로 실행하면 이렇게 멈춥니다.

```
ERROR 1054 (42S22): Unknown column 'artists' in 'field list'
```

작은따옴표로 바꾸면 정상 실행됩니다. 세 번째 줄 끝의 역슬래시도 함께 지웁니다.
**바로 쓸 수 있는 SQL은 `5주차/환경셋업/도커로_MySQL_준비하기.md` 3단계에 있습니다.**

```sql
SELECT 'artists' AS table_name, COUNT(*) AS row_count FROM artists
UNION ALL SELECT 'fans', COUNT(*) FROM fans
UNION ALL SELECT 'tracks', COUNT(*) FROM tracks
UNION ALL SELECT 'streaming', COUNT(*) FROM streaming
UNION ALL SELECT 'orders', COUNT(*) FROM orders
UNION ALL SELECT 'subscription_history', COUNT(*) FROM subscription_history
UNION ALL SELECT 'fan_activities', COUNT(*) FROM fan_activities
UNION ALL SELECT 'query_history', COUNT(*) FROM query_history;
```

## 오류를 발견하셨다면

[교재 본문 오류·오탈자 제보](https://github.com/hansungc/sql-to-ai/issues/new?template=2-book-errata.yml)에
남겨 주세요. 쪽수와 인쇄된 내용을 함께 적어 주시면 빠르게 확인할 수 있습니다.

확인이 끝나면 이 문서에 추가하고
[Announcements](https://github.com/hansungc/sql-to-ai/discussions/categories/announcements)에
공지합니다.

예제 파일이 실행되지 않는 문제는 지면 오류가 아니라
[예제 파일 오류 제보](https://github.com/hansungc/sql-to-ai/issues/new?template=1-example-bug.yml)로
남겨 주세요. 파일을 고쳐 저장소에 바로 반영합니다.

## 읽는 법

<!--
확인된 항목은 아래 형식으로 추가합니다.

## 1쇄

| 쪽 | 위치 | 인쇄된 내용 | 바른 내용 |
|---:|---|---|---|
| 356 | Day 16 예제 3 결과 표 | track_id 7, 8 | track_id 34, 40 |
-->

쪽수는 **책장에 인쇄된 쪽번호** 기준입니다. 쇄가 바뀌면 절이 나뉘므로, 어느 쇄를 보고
계신지는 판권면(맨 뒷장)에서 확인하실 수 있습니다.
